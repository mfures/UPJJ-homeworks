package hr.fer.zemris.java.hw06.shell;

/**
 * Enumeration that describes shells statuses
 * 
 * @author Matej Fureš
 *
 */
public enum ShellStatus {
	/**
	 * Used if shell should continue with its work
	 */
	CONTINUE,

	/**
	 * Used if shell should terminate its work
	 */
	TERMINATE;
}
